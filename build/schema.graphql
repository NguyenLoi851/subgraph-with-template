type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  length: BigInt! # uint256
}

type AbcEntity @entity {
  id: ID!
  length: BigInt!
}

type CreateNewCampaignEntity @entity {
  id: ID!
  aNumber: BigInt
  bAddress: Bytes
  campaign: Bytes
}

type MultipleCampaignEntity @entity {
  id: ID!
  aNumber: BigInt
  kNumber: BigInt
  resNumber: BigInt
}